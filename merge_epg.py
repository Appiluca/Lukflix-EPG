import urllib.requestimport xml.etree.ElementTree as ETimport gzipimport ioimport sysURL_LAND1 = "https://epg.lat/files/de.xml.gz"URL_LAND2 = "https://epg.lat/files/ch.xml.gz"OUTPUT_FILE = "epg.xml"def main(): print("Starte EPG Download...") headers = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)'} try: # Erste Datei (DE) laden req1 = urllib.request.Request(URL_LAND1, headers=headers) with urllib.request.urlopen(req1) as response: with gzip.GzipFile(fileobj=io.BytesIO(response.read())) as gz: tree1 = ET.parse(gz) root1 = tree1.getroot() print("EPG 1 (DE) erfolgreich geladen.") # Zweite Datei (CH) laden req2 = urllib.request.Request(URL_LAND2, headers=headers) with urllib.request.urlopen(req2) as response: with gzip.GzipFile(fileobj=io.BytesIO(response.read())) as gz: tree2 = ET.parse(gz) root2 = tree2.getroot() print("EPG 2 (CH) erfolgreich geladen.") print("Führe EPG-Daten sauber zusammen...") # Bestehende Channel-IDs aus der ersten Datei erfassen, um Duplikate zu vermeiden existing_channels = set() for channel in root1.findall('channel'): channel_id = channel.get('id') if channel_id: existing_channels.add(channel_id) # Listen für die neuen Elemente aus der zweiten Datei new_channels = [] new_programmes = [] # Elemente der zweiten Datei sortieren for child in root2: if child.tag == 'channel': channel_id = child.get('id') if channel_id not in existing_channels: new_channels.append(child) existing_channels.add(channel_id) elif child.tag == 'programme': new_programmes.append(child) # Index bestimmen, wo die Programme in der ersten Datei beginnen # Neue Channels müssen VOR den ersten Programmen eingefügt werden insert_index = 0 for i, child in enumerate(root1): if child.tag == 'programme': insert_index = i break else: insert_index = len(root1) # Neue Kanäle an der richtigen Position einfügen for channel in reversed(new_channels): root1.insert(insert_index, channel) # Neue Programme einfach ganz ans Ende anhängen for programme in new_programmes: root1.append(programme) print("Dateien erfolgreich und strukturiert zusammengeführt.") # Speichern tree1.write(OUTPUT_FILE, encoding='utf-8', xml_declaration=True) print(f"Datei '{OUTPUT_FILE}' erfolgreich generiert.") except Exception as e: print(f"Fehler während des Prozesses: {e}") sys.exit(1)if __name__ == "__main__": main()
